<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.authservice.mapper.OAuth2UserMapper">
    <resultMap id="userResultMap" type="User">
        <result property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="role" column="role"/>
        <result property="provider" column="provider"/>
        <result property="providerId" column="provider_id"/>
        <result property="profileImage" column="profile_image"/>
        <result property="status" column="status"/>
        <result property="lastLogin" column="last_login"/>
        <result property="isVerified" column="is_verified"/>

    </resultMap>

    <select id="findByUserIdAndProvider"
            parameterType="map"
            resultMap="userResultMap">
        SELECT *
        FROM users
        WHERE provider_id = #{providerId} AND provider = #{provider};
    </select>


    <insert id="insertOAuthUser" parameterType="User">
        INSERT INTO users (provider_id, nickname, phone_number, email, provider)
        VALUES (#{providerId}, #{nickname}, #{phoneNumber}, #{email}, #{provider};
    </insert>

</mapper>



